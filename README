A simple command line utility that converts an ONNX file (such as one exported from pytorch) describing a neural network, and compiles it to a linkable binary.

Currently this repo contains some initial experiments with deserializing and processing ONNX files. Please check back later!